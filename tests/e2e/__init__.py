# tests/e2e/__init__.py
#
# Tests end-to-end de RenderBrain — requieren PostgreSQL y Redis en ejecución.
# Ejecutar con:  uv run pytest tests/e2e -v -m integration
# Excluir con:   uv run pytest -m "not integration"
