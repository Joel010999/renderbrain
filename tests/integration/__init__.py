# tests/integration package
#
# Tests en este directorio requieren un PostgreSQL real en ejecución.
# Ejecutar con:  uv run pytest tests/integration -v -m integration
# Excluir con:   uv run pytest -m "not integration"
